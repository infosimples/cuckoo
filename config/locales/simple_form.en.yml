en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "Please review the problems below:"
    # Labels and hints examples
    # labels:
    #   defaults:
    #     password: 'Password'
    #   user:
    #     new:
    #       email: 'E-mail to sign in.'
    #     edit:
    #       email: 'E-mail.'
    # hints:
    #   defaults:
    #     username: 'User name to sign in.'
    #     password: 'No special characters, please.'
    labels:
      defaults:
        started_at: 'Start'
        ended_at: 'End'
  activerecord:
    errors:
      models:
        time_entry:
          attributes:
            base:
              entries_that_dont_start_today_must_have_start_and_end: "Entries that aren't from today must have start and end time"
              start_and_end_must_be_in_order: "Start and end must be in order"
              start_and_end_must_be_same_day: "Start and end must be the same day"